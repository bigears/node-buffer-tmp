{
  "name": "buffer-tmp",
  "version": "1.1.1",
  "description": "Pipe a stream to a temp file then get a read stream back on the same file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.1.1",
    "debug": "^2.2.0",
    "tmp": "0.0.28"
  }
}
